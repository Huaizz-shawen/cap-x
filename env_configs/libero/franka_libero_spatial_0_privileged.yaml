# LIBERO Spatial Task 0 with privileged API
# Usage:
#   uv run capx/envs/launch.py --config-path env_configs/libero/franka_libero_spatial_0_privileged.yaml

env:
  _target_: capx.envs.tasks.franka.franka_libero_env.FrankaLiberoCodeEnv
  cfg:
    _target_: capx.envs.tasks.base.CodeExecEnvConfig
    low_level:
      _target_: capx.envs.simulators.libero.FrankaLiberoEnv
      suite_name: libero_spatial
      task_id: 0
      privileged: true
      max_steps: 8000
      control_freq: 20
    privileged: true
    apis:
      - FrankaLiberoPrivilegedApi
    prompt: |
      You are controlling a Franka Emika robot with API described below.
      Goal: {libero_environment_goal}
      When the goal names an object using attributes, state, or spatial relations, you must preserve that full discriminative phrase in your internal target selection logic. Do not simplify "the black bowl between the plate and the ramekin" to "bowl".
      You may write python code comments for reasoning but ONLY write the executable Python code and do not write it in code fences.
      After this code executes, you will be able to get a new observation of the environment and write new code to complete the task if needed.
      The functions (APIs) below are already imported to the environment. If you want to use numpy, you need to import it explicitly.

api_servers:
  - _target_: capx.serving.launch_pyroki_server.main
    port: 8116
    host: 127.0.0.1
    robot: panda_description
    target_link: panda_hand

record_video: true
output_dir: ./outputs/franka_libero_spatial_0_privileged

trials: 50
num_workers: 10
